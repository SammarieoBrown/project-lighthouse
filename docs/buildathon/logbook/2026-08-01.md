# Logbook — Saturday, August 1, 2026

Foundations day: provisioned the stack, audited the full spec set against itself, closed every open decision, then froze the contracts and built the spine. Ended the day with the hardest Phase 0 exit criterion met.

## Progress

**Infrastructure provisioned.** Neon serverless Postgres stood up in `us-east-2` and verified against the live instance rather than assumed: PostgreSQL 18.4, empty schema, PostGIS 3.6.0 and pgvector 0.8.1 available but not yet installed — so the initial migration has to create both. Our docs had said Postgres 16 on Render; corrected everywhere.

**Twilio WhatsApp sandbox live.** Sandbox number and join code configured, one Jamaican number (`+1876…`) enrolled as a participant. The inbound webhook still points at Twilio's default demo responder and gets repointed at our API once Render is up.

**Architecture settled.** Vercel for the EOC console and public portal, Render for the API (`web` + `worker` entrypoints), Neon for Postgres. This also resolved a live contradiction in our own docs, where the build spec described five deployable services while the phase plan and the committed scaffolding described two.

**Documentation reconciled and merged.** PRD v1.1 on `main`, plus a new environment/integrations runbook.

**Contract freeze complete — the four artifacts everything else depends on.** The canonical schema, the state transition table, Pydantic I/O contracts for all eight agents, and the event catalogue. The agent contracts double as the JSON schema for structured model output, so the contract and the prompt cannot drift apart. This is what lets three people work two weeks in parallel without a merge conflict that matters.

**The spine is running, and Phase 0 exit criterion #2 is met:** an integration test drives one synthetic Storm File through all five states and the ledger hash chain validates. 11 tests green. Migration applied to Neon — 23 tables. `/health` returns 200 with a valid chain, and the WhatsApp webhook durably queues an inbound message and returns immediately.

Two guarantees are enforced in the database rather than in application code, and both are tested firing: a disbursement row cannot exist before a Finance Officer has signed, and a Storm File cannot reach SETTLED without a confirmed payment — bypassing the state machine with raw SQL fails too. "Agents propose, humans dispose" is now a property of the schema, not a rule someone has to remember at 2am in week three.

Writing the tests found two bugs immediately: a claim could never legally be created because creation was modelled as a transition when it is not one, and `python-multipart` was missing — Twilio posts form-encoded, so every inbound WhatsApp message would have failed on the first real send. Better found tonight than in rehearsal week.

## Decisions

1. **Job queue: Postgres `SKIP LOCKED`, not Redis.** Enqueueing an agent job and writing the state transition it follows from happen in one transaction. With a separate queue they cannot — a Storm File could change state while its follow-on job is silently lost, producing an orphaned claim with nothing in the ledger recording the failure. That is the exact outcome this platform exists to prevent, so we removed the possibility structurally rather than monitoring for it.

2. **Time to Relief redefined.** Was *verified claim → relief*, which started the clock after verification — the hard part, and the part a finance or audit audience will interrogate. Now *claim filed → first relief confirmed*. Measured against our replay this moves the median from 34h to 39h against a 72h target. We gave up five hours of headline for a number that survives the obvious question.

3. **Scope gap found and closed.** Payer routing, the insurer FNOL packet, and the donation → donor-journey path were all marked P0 in the PRD and appear as steps 4, 6 and 8 of our own acceptance test — but appeared nowhere in the build plan and had no owner. Found in a cross-document audit. All three now assigned across week 3 and marked not-cuttable; the insurer portal and portfolio heatmap stay out of scope at P1.

4. **Relief amounts:** flat J$45,000 cash grant per verified household, plus goods tiered by triage severity. Flat cash keeps any per-claim valuation judgement out of the agent path.

5. **A fictional insurer ("Island Mutual") in all demo UI.** Real carriers appear only in market sizing, so nothing on screen implies an endorsement we do not have.

6. **Privacy:** the pre-landfall anticipatory list is Director-role only. It is a ranked register of vulnerable people and where they live; publishing it would invert the privacy posture the rest of the platform argues for. Transparency obligations are met by aggregate-only public reporting.

## Findings and risks

**The WhatsApp 24-hour session window changes our demo run sheet.** Act 1 sends a free-form Patois alert cascade. A judge's phone has never messaged us, so at Act 1 it sits outside WhatsApp's 24-hour customer-service window, where only pre-approved templates may be sent — the alert would not deliver. The fix is sequencing, not engineering: the judge joins the sandbox *before Act 1* rather than before Act 2, which both enrols them and opens the window. This is the class of bug that works on every team phone and fails on the one phone that matters, so it gets tested well before rehearsal week.

**Highest-risk demo dependency: sandbox international delivery.** Twilio warns that sandbox delivery to international numbers is best-effort, and our users and demo are Jamaican numbers on a US sandbox number. Mitigations agreed: repeated testing from Jamaican numbers at varied times, Meta Cloud API business verification pursued in parallel, a pre-recorded voice-note fallback kept one keypress away, and a rehearsed threshold at which the presenter switches to it without commentary.

**Trial-account artefacts.** Outbound SMS currently arrives prefixed with "Sent from your Twilio trial account -", and trial accounts can only SMS pre-verified numbers. Both need clearing before demo day.

## Blockers

- Meta WhatsApp Cloud API business verification has external lead time outside our control. We are planning as though it does not land; Twilio remains the demo path, and both providers sit behind one internal messaging interface so switching is configuration rather than a rewrite.
- Phase 0 stands at 1 of 5 exit criteria. The remaining four — Docker Compose from a clean clone, the webhook reachable on Render over HTTPS, the console deployed on Vercel, and all three developers pushing with CI green — are mechanical now that the spine exists, except the last, which needs the other two developers.
- Only one of us has pushed code so far. Phase 0 exists so that nobody can block anybody afterwards, and that is not true until Raheem and Matthew have both built against the frozen contracts.

## Customer conversations

None today — the day was internal foundations. Outreach to a parish council and an NGO partner is planned as pilot-readiness work rather than buildathon-week work.

## Next

1. **Send the Patois evaluation utterance list.** It depends on other people finding time to record audio, so it has the longest lead time of anything on this list and the lowest cost to start. It should have gone out today.
2. Raheem and Matthew review the frozen contracts and build against them — agents against the Pydantic models with a fake orchestrator, console against generated TypeScript types and seeded rows. Neither needs the other.
3. Docker Compose, CI, and Render/Vercel provisioning; repoint both Twilio webhooks including the status callback that per-recipient delivery tracking depends on. Takes Phase 0 to 4 of 5.
4. Melissa advisory cache committed with a checksum manifest, so the replay is reproducible on every machine rather than on whichever laptop last fetched it.
5. Settle the disputed damage figure in the pitch narrative before anyone records a video against it.
