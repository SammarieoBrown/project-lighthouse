import { ReliefOperations } from "./relief-operations";

export const metadata = {
  title: "Relief operations · Lighthouse",
  description: "Redacted claim intake, human allocation approval, and public ledger proof.",
};

export default function OperationsPage() {
  return <ReliefOperations />;
}
